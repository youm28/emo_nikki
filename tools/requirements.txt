# リマインダー送信に必要なパッケージ。
#
# 版を固定するのは、firebase-admin が更新された日に、こちらは何も
# 変えていないのに通知が止まるのを防ぐため。実際 7.5.0 の時点で
# Message.token が非推奨になっており、いずれ削除される見込み。
#
# 上げるときは、必ず --force で1回送って通知が届くことを確かめてから。
# 実験期間中は上げない。
firebase-admin==7.5.0
